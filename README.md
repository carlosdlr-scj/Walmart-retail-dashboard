# Retail Business Analytics — Streamlit

Multipage Streamlit dashboard based on the Walmart Retail Data file used in the project.

## Run locally
```bash
pip install -r requirements.txt
streamlit run streamlit_app.py
```

The five analytical pages are stored in the `pages/` folder.
